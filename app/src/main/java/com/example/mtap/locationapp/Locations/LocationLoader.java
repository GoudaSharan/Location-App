package com.example.mtap.locationapp.Locations;

import android.content.Context;
import android.support.v4.app.LoaderManager;

import com.example.mtap.locationapp.framework.AbstractLoader;

public class LocationLoader extends AbstractLoader {

    public LocationLoader(Context context, LoaderManager supportLoaderManager) {
        super(context, supportLoaderManager);
    }
}